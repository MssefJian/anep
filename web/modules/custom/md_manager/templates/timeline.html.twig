<section class="timeline">
  <div class="container">
    {% for key, timeLine in timeLines %}
    <div class="timeline-item">
      <div class="timeline-img"></div>

      <div class="timeline-content js--{{ key is odd ? 'fadeInLeft' : 'fadeInRight'}}">
        <div class="date">{{ timeLine.year }}</div>
        <p>{{ timeLine.title }}</p>
      </div>
    </div>
    {% endfor %}




  </div>
</section>
